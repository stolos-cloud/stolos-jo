apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
    name: edvin-team-apps
    namespace: argocd
spec:
    generators:
        - git:
            repoURL: https://github.com/stolos-cloud/stolos-jo
            revision: HEAD
            directories:
                - path: namespaces/edvin-team/apps/*
    template:
        metadata:
            name: edvin-team-{{path.basename}}
        spec:
            project: default
            source:
                repoURL: https://github.com/stolos-cloud/stolos-jo
                targetRevision: HEAD
                path: '{{path}}'
            destination:
                server: https://kubernetes.default.svc
                namespace: app-edvin-team
            syncPolicy:
                automated:
                    prune: true
                    selfHeal: true
                syncOptions:
                    - CreateNamespace=true
