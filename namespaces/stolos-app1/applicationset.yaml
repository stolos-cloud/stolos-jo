apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
    name: stolos-app1-apps
    namespace: argocd
spec:
    generators:
        - git:
            repoURL: https://github.com/stolos-cloud/stolos-jo
            revision: HEAD
            directories:
                - path: namespaces/stolos-app1/apps/*
    template:
        metadata:
            name: stolos-app1-{{path.basename}}
        spec:
            project: default
            source:
                repoURL: https://github.com/stolos-cloud/stolos-jo
                targetRevision: HEAD
                path: '{{path}}'
            destination:
                server: https://kubernetes.default.svc
                namespace: app-stolos-app1
            syncPolicy:
                automated:
                    prune: true
                    selfHeal: true
